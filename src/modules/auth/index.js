const AuthControllers = require('./auth.controllers');
const AuthRoutes = require('./auth.routes');
const AuthProviders = require('./auth.providers');

module.exports = { AuthControllers, AuthRoutes, AuthProviders };
